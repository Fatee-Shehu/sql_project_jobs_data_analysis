/* question 5: what are the optimal skills to learn
- identify skills that are in high demand with high salaries
- concentrates on remotes positions with specified salaries
- reason; target skills that give job security(high demand skills) and finacial benefits (high salaries)
*/ 
 
-- alternatively
 select skills_dim.skill_id,
        skills_dim.skills,
        count(skills_job_dim.job_id) as demand_count,
        round (avg(salary_year_avg), 0) as avg_salary
 from job_postings_fact
      INNER JOIN skills_job_dim ON job_postings_fact.job_id = skills_job_dim.job_id
      inner join skills_dim on skills_job_dim.skill_id = skills_dim.skill_id
where job_title_short = 'Data Analyst' 
      and job_work_from_home = 'true'
      and salary_year_avg is not null
group by skills_dim.skill_id
having count(skills_job_dim.job_id) > 10
order  by
        avg_salary desc,
        demand_count desc
limit 25;        
