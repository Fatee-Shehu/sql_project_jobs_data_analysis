/* question 3: find the most in-demand skills for data analyst role
-inner join job postings to the joins of query 2
identify the top 5 in demand skills 
-focusing on job postings
-this provides insights on most valuable skills for job seekers
*/
select 
       skills,
       count(skills_job_dim.job_id) as demand_count
 from job_postings_fact
      INNER JOIN skills_job_dim ON job_postings_fact.job_id = skills_job_dim.job_id
      inner join skills_dim on skills_job_dim.skill_id = skills_dim.skill_id
where job_title_short = 'Data Analyst' and 
      job_work_from_home = 'true'
group by skills
order by demand_count desc      
limit 5;